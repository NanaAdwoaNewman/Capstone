runtime: php74

runtime_config:
  document_root: .

handlers:
# Serve images as static resources.
- url: /(.+\.(gif|png|jpg|css|js|map|PNG))$
  static_files: \1
  upload: .+\.(gif|png|jpg|css|js|map|PNG)$

- url: /.*
  script: auto

env_variables:
  # Replace USER, PASSWORD, DATABASE, and CONNECTION_NAME with the
  # values obtained when configuring your Cloud SQL instance.
  CLOUDSQL_USER: root
  CLOUDSQL_DB: NanaAdwoa
  CLOUDSQL_PASSWORD: Adw0@2019
  CLOUDSQL_DSN: /cloudsql/lofty-root-382210:us-central1:nanacapstone